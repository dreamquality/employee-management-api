# Database Configuration
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=employee_db
DB_USER=your_local_db_user
DB_PASSWORD=your_local_db_password

# Authentication Secrets
JWT_SECRET=your_jwt_secret
SECRET_WORD=your_secret_word_for_admin_registration

# Application
NODE_ENV=development
PORT=3000

# CORS
CORS_ORIGIN=http://localhost:5173

# ==============================================================================
# SMTP Email Configuration
# ==============================================================================
# Choose one of the configurations below based on your environment:

# --- OPTION 1: MailHog (Development/Testing) ---
# MailHog is a local email testing tool that captures emails without sending them
# Use this for development and testing
# View captured emails at: http://localhost:8025
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_FROM=noreply@employee-crm.com
# SMTP_USER=          # Leave empty for MailHog
# SMTP_PASS=          # Leave empty for MailHog
# SMTP_SECURE=false   # MailHog doesn't use TLS

# --- OPTION 2: Gmail (Production) ---
# Uncomment and configure these lines to use Gmail
# Note: You need to generate an "App Password" from your Google Account settings
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_FROM=your-email@gmail.com
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_SECURE=true

# --- OPTION 3: SendGrid (Production) ---
# Uncomment and configure these lines to use SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_FROM=noreply@yourdomain.com
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key
# SMTP_SECURE=true

# --- OPTION 4: AWS SES (Production) ---
# Uncomment and configure these lines to use AWS SES
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_FROM=verified-email@yourdomain.com
# SMTP_USER=your-ses-smtp-username
# SMTP_PASS=your-ses-smtp-password
# SMTP_SECURE=true

# --- OPTION 5: Other SMTP Service (Production) ---
# Configure your custom SMTP service
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_FROM=noreply@yourdomain.com
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# SMTP_SECURE=true
